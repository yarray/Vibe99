Add `getActiveId()` method to `FocusController` interface and implementation for proper type safety.
All Phase 2 capability `open()` methods return explicit interfaces (no `unknown`).
No `any` types or `@ts-ignore` directives in Phase 2 modules.
