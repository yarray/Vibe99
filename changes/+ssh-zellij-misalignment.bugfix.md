Fix PTY size synchronization for SSH+zellij scenarios. The previous implementation called fitAddon.fit() before the layout stabilized, leading to inaccurate terminal dimensions being passed to the backend PTY. This caused permanent display misalignment in SSH+zellij sessions. The fix waits for two animation frames before creating the PTY to ensure layout has fully stabilized, then performs a final sync after the PTY is ready.
