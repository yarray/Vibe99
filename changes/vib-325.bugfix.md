Fix `layoutHotkeys` type mismatch in `sanitize_ui_config` that caused CI to fail on main after PR #342. The `Map<String, String>` was corrected to `Map<String, Value>` to match `serde_json::Value::Object`'s expected type.
