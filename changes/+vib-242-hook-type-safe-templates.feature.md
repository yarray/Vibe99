Hook parameter system refactor: typed event payloads (`AlertStartPayload`, `AlertStopPayload`, `HookEventMap`), safe mustache-style template rendering with cross-platform shell escaping via [shescape](https://github.com/ericcornelissen/shescape) (bash, zsh, sh, cmd.exe, PowerShell), and on-demand terminal buffer reading via `PaneRenderer.getRecentOutput()`. Hook commands now support `{{paneId}}`, `{{paneTitle}}`, and `{{recentOutput}}` variables.
