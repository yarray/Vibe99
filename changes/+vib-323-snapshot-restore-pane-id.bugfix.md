Fixed pane content loss when switching layouts: `restoreSession()` now preserves original pane IDs instead of renumbering, preventing `ensureSessions()` from destroying and recreating TerminalSessions with empty buffers. Removed dead `sessionMap` legacy path from pane-renderer (workbench is always provided). Simplified `nextPaneNumber` recalculation after restore — replaced regex with direct `parseInt`.
