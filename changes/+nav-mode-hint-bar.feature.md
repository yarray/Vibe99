Navigation mode now displays mode-specific keyboard hints in the status bar. Press `Esc` to exit navigation mode and return focus to the source pane. Added "Compact hint bar" toggle in settings to minimize the hint display.
