- **Per-pane activity alert runtime sync (VIB-280):** Fixed three-way inconsistency between persistent state, context menu display, and watcher live state. New panes now default to activity alert enabled (`breathingMonitor: true`). Toggling per-pane alert immediately syncs the watcher and clears visual alerts without requiring session rebuild or layout switch.
