Created `src/pane/capabilities/activity-capability.ts` (50 lines) and `src/pane/capabilities/clipboard-capability.ts` (80 lines) by extracting pane-activity-watcher interactions and clipboard logic from `pane-renderer.ts`. `activity-capability.ts` provides `noteOutput()`, `setEnabled()`, `setAlerted()`, and `close()` via `createActivityBehavior(deps)`. `clipboard-capability.ts` provides `open()`, `paste()`, `readClipboard()`, and `snapshot()` via `createClipboardBehavior(deps)`, including OSC 52 clipboard handler and selection-changed auto-copy.
