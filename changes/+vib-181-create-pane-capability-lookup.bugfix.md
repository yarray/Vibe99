Fixed `capability(name)` lookup bug: now reads from internal `capabilities` Map populated by `open()` return values, instead of searching behavior object property keys. `open()` collects each behavior's API return value (keyed by `behavior.name`) into the Map. `PaneHandle` now exposes `capability()` and `emit()` for inter-capability lookups during initialization. `PaneBehavior.name` is required; removed unsafe index signature.
