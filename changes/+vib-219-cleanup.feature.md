Remove legacy monoliths `src/bridge.ts`, `src/pane-state.ts`, and `src/pane-renderer.ts`. `backend.ts` is now self-contained with all core IPC types (259 lines). `compat/bridge-compat.ts` absorbed the `Bridge` interface, `createBridge()` factory, and layout window binding utilities. `compat/pane-state-compat.ts` inlined `Pane`, `PaneState`, `SessionData`, `SessionPaneEntry`, `PaneStateDeps`. `compat/pane-renderer-compat.ts` holds `PaneNode`, `PaneRenderer`, `createPaneRenderer()`, and `getTextColorForBackground()`. Updated all 10 consumer files to import from compat layers. `renderer.ts` remains 77 lines; all core new-architecture files stay ≤300 lines. `npx tsc --noEmit` and `npm run vite:build` pass. (VIB-219)
