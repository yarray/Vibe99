Fix 3 e2e test failures in Phase 2 pane-capability refactor: (1) `restoreSession` in pane-state-compat now initializes FocusController's MRU order with focused pane first then remaining panes in tab order, matching original pane-state.ts behavior; (2) `clearPaneColor` now syncs with PaneManager via `handle?.setState({ customColor: undefined })`, previously only updated local state; (3) Initial panes created on empty session restore now get MRU order initialized. These fixes ensure Ctrl+Tab cycling works correctly after session restore and custom color clearing persists properly. (VIB-222)
