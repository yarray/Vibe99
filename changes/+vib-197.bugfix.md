Fix visual artifacts when adjusting pane width. The terminal resize is now debounced during slider drag to prevent rapid resize operations that corrupt the xterm.js buffer. CSS updates apply immediately for smooth visual feedback, but the expensive terminal refit only happens after the user stops dragging (150ms delay).
