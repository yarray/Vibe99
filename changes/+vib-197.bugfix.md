Fix visual artifacts when adjusting pane width. Terminal resize now only happens when the slider is released (change event), not during dragging (input event). CSS updates apply immediately for smooth visual feedback, but the expensive terminal refit is deferred to avoid xterm.js buffer corruption from rapid resize operations.
