Fix visual artifacts when adjusting pane width. The xterm.js `fit()` call now happens in the next animation frame after CSS layout changes, ensuring the terminal correctly reflows without residual content above the viewport.
