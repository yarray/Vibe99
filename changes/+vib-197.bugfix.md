Fix visual artifacts when adjusting pane width. During slider drag, pane positions update in real-time for visual feedback, but terminal resize is deferred until release to prevent xterm.js buffer corruption. On resize, a full redraw is forced by temporarily expanding then shrinking the terminal width to clear rendering cache artifacts.
