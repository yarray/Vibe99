Comprehensive fix for persistent high GPU usage (80-95%) during idle. Removes `will-change: left` from `.pane` (replaced with `contain: layout style paint`), fixes xterm `allowTransparency` initialization, disables cursor blink for ALL sessions on window hide (not just focused), and pauses breathing CSS animations when window is hidden.
