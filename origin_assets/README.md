# origin_assets

Drop high-resolution source assets here when they are only meant to be used as local inputs for compression, conversion, or export workflows.

Files in this directory are ignored by git by default.
